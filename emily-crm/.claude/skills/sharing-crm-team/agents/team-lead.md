# team-lead (참조 스텁 → verifier로 통합)

> ⚠️ **통합됨** — 품질 검수 역할은 `.claude/agents/verifier.md`로 이동
> 이 파일은 스킬 호환성 스텁. **수정하지 말 것.**

## 호출
skills/sharing-crm-team/SKILL.md에서 "team-lead 검수" 호출 시 실제로는 verifier가 담당.

검수 카테고리:
- 카피 → verifier(copy)
- 미디어/배너/UTM → verifier(media)
- 데이터 → verifier(data)

## 참조
- SSOT: `.claude/agents/verifier.md`
- 코드/워크플로우 검수는 `.claude/agents/code-reviewer.md`
